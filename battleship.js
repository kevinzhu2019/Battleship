const battleShip = () => {
  const battleField = [];
  battleField.push('a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i', 'j');
  console.log(battleField);
  for (let i = 0; i < battleField.length; i++) {
    
  }
};

battleShip();